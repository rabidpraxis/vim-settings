set guifont=Inconsolata:h15
colorscheme molokai
set background=dark
if has("gui_running")
    set guioptions-=T
    set guioptions-=r
endif
" In many terminal emulators the mouse works just fine, thus enable it.
if has('mouse')
  set mouse=a
endif


